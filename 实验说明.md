## 1. 编译

使用不同的开发板、不同的编译工具时，
需要灵活变通：

1. 视频里使用的工具链是 arm-linux-gcc，如果你的工具链不同，请自行修改

### 1.1 对于IMX6ULL

使用Makefile的程序，要修改Makefile：

* 工具链前缀：可以继续使用"arm-linux-"，也可以使用"arm-buildroot-linux-gnueabihf-"



### 1.2 对于STM32MP157

使用Makefile的程序，要修改Makefile：

* 工具链前缀：可以继续使用"arm-linux-"，也可以使用"arm-buildroot-linux-gnueabihf-"



## 2. 开发板网络配置

在开发板的《嵌入式Linux应用开发完全手册》中有介绍如何配置网卡，使得可以跟Ubuntu联通。
简介如下：

### 2.1 IMX6ULL

修改开发板的/etc/network/interfaces，内容如下：

```shell
auto lo
iface lo inet loopback
auto eth0
iface eth0 inet static
    address 192.168.5.9
    netmask 255.255.255.0
    gateway 192.168.5.1
```

然后重启。

### 2.2 STM32MP157

修改开发板的/etc/systemd/network/50-static.network，内容如下：

```shell
[Match]
Name=eth0
[Network]
Address=192.168.5.9/24
Gateway=192.168.5.1
```

然后重启。



## 3. 移除GUI

### 3.1 IMX6ULL

在开发板上执行如下命令后重启:

```shell
mv  /etc/init.d/S99myirhmi2  /root
mv  /etc/init.d/S05lvgl  /root
```



### 3.2 STM32MP157

在开发板上执行如下命令:

```shell
systemctl stop myir
```



## 4. LCD黑屏

实验过程中LCD过几分钟就黑屏，这是正常的。
可以关闭黑屏的功能，执行以下命令即可：

```shell
echo -e "\033[9;0]" > /dev/tty0
```



## 5. 嵌入式Linux相机上机实验

把最终源码上传到Ubuntu：

```shell
doc_and_source_for_projects\02_Linux项目\02_毕业设计级别项目\05_嵌入式Linux相机\02_嵌入式Linux相机\源码\6th_ok

doc_and_source_for_projects\02_Linux项目\02_毕业设计级别项目\05_嵌入式Linux相机\02_嵌入式Linux相机\源码\7th_基于LVGL实现相机\lv_100ask_camera.tar.bz2
```



解压后执行make命令即可。



然后在开发板上插上摄像头，然后运行程序：

对于IMX6ULL，执行如下命令：

```shell
./video2lcd  /dev/video1
```



对于STM32MP157，执行如下命令：

```shell
./video2lcd  /dev/video0
```



## 6. 笔记

怎么确定工具链里头文件、库文件存放在哪里？

以 IMX6ULL 开 发 板 为 例 ， 它 的 工 具 链 是 arm-buildroot-linuxgnueabihf-gcc，可以执行以下命令：

```c
echo 'main(){}'| arm-buildroot-linux-gnueabihf-gcc -E -v -
```

就可以确定头文件的系统目录为：

```c
/home/book/100ask_imx6ull-sdk/ToolChain/arm-buildroot-linux-gnueabihf_sdk-buildroot/
bin/../lib/gcc/arm-buildroot-linux-gnueabihf/7.5.0/include
```



库文件的系统目录为：

```shell
/home/book/100ask_imx6ull-sdk/ToolChain/arm-buildroot-linux-gnueabihf_sdk-buildroot/
bin/../lib/gcc/arm-buildroot-linux-gnueabihf/7.5.0/../../../../arm-buildroot-linux-g
nueabihf/lib
```



##   7. LVGL特别说明

本项目并没有讲解LVGL，里面的LVGL源码仅供参考。

如果要了解更多内容，可以查看如下GIT仓库：

```shell
lv_100ask_linux_camera仓库地址：
https://gitee.com/weidongshan/lv_100ask_linux_camera
https://github.com/100askTeam/lv_100ask_linux_camera
```



